import 'package:flutter/material.dart';

class ScreenParkinson extends StatelessWidget {
  const ScreenParkinson({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ScreenParkinson'),
      ),
      body: Center(
        child: Text('ScreenParkinson'),
      ),
    );
  }
}
